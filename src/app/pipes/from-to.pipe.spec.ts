/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FromToPipe } from './from-to.pipe';

describe('FromToPipe', () => {
  it('create an instance', () => {
    let pipe = new FromToPipe();
    expect(pipe).toBeTruthy();
  });
});
