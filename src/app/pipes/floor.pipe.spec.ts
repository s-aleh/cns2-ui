/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FloorPipe } from './floor.pipe';

describe('FloorPipe', () => {
  it('create an instance', () => {
    let pipe = new FloorPipe();
    expect(pipe).toBeTruthy();
  });
});
