/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MonthsPipe } from './months.pipe';

describe('MonthsPipe', () => {
  it('create an instance', () => {
    let pipe = new MonthsPipe();
    expect(pipe).toBeTruthy();
  });
});
